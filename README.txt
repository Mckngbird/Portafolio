html:

-utilicé la etiqueta section que es para agrupar contenido específico, como en este caso para las distintas descripciones de cada parte de la páagina

css:

-utilicé ul.list-inline li: el cual permite aplicar estilos a todos los elementos que se encuentren dentro de nuestra lista <ul>

js:

-utilicé una expresion regular que asegura que pertenezca a un email, asegurandose que existan carácteres dentro de la cadena que exista un @ y que haya un punto despues de este.
-Trim por si el usuario utilizo espacios antes o despues de escribir su nombre, mensaje o correo.
-Test devuelve un valor booleano si el correo tiene el formato evaluado por la expresión regular, si no se valida en cada uno de los casos 

-utilicé window.location.href indicando que vaya al archivo que se encuentra en mi carpeta assets, donde se encuentra mi curriculum para iniciar la descarga del archivo.
